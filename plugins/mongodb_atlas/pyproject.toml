[project]
name = "opensre-plugin-mongodb-atlas"
version = "0.1.0"
description = "OpenSRE mongodb_atlas integration plugin (5 tool(s), bridged from core)"
requires-python = ">=3.12"
dependencies = [
    "opensre-plugin-sdk",
]

[project.optional-dependencies]
opensre = ["opensre>=0.1"]

[tool.opensre-plugin]
name = "mongodb_atlas"
tools_package = "mongodb_atlas_plugin.tools"
description = "Bridged mongodb_atlas tools for OpenSRE investigations"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mongodb_atlas_plugin"]
